name: damnit-web-local

services:
  api:
    build:
      context: ..
      dockerfile: api/Dockerfile
    volumes:
      - ./tmp-damnit-web/:/tmp/damnit-web/
      - ./certs:/certs
    ports:
      - 8000:8000
